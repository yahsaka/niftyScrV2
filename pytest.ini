[pytest]
testpaths = tests
pythonpath = .
addopts = -ra
markers =
    app: real Streamlit AppTest integration; requires runtime dependencies
    browser: real browser integration; requires a running app
